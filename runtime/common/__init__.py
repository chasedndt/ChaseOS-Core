"""runtime.common — small dependency-free utilities shared across ChaseOS Core modules.

Stdlib-only helpers with no cross-module runtime dependencies, so they are safe to
include in the public MIT ChaseOS Core and depended on by other Core modules and
adapters without creating coupling to proprietary or excluded layers.
"""
